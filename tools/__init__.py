"""
Standalone tools built on top of the provider registry.

- cascade: Production-ready cascade client with retry and failover
- compare: Side-by-side comparison of provider responses
- proxy: Local OpenAI-compatible proxy that routes to free providers
- rate_limiter: Per-provider rate limiting and quota tracking
- conversation: Chat history save/load/export (JSON, Markdown)
- token_counter: Estimate token counts without external dependencies
- cost_calculator: Compare pricing across providers and estimate costs
"""
